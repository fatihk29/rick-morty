import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  itemContainer: {
    marginVertical: 6,
    marginHorizontal: 8,
    borderRadius: 10,
    opacity: 0.5,
  },
});

export default style;
